export interface User {
  firstName: string;
  lastName: string;
  postalCode: string;
}

export const UserData: User = {
  firstName: 'Juan',
  lastName: 'Coronado',
  postalCode: '110111',
};